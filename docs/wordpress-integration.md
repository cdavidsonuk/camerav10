# WordPress Integration Foundation

Phase 4 emits `photographycourses:progress-ready` with the endpoint, course ID and local progress. A production WordPress plugin must add authentication, server-side validation and storage.
